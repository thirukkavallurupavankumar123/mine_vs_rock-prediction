# mine_vs_rock-prediction
This project focuses on building a machine learning model to classify sonar signals as either mines or rocks. The dataset used is the Sonar Dataset from the UCI Machine Learning Repository, which contains 60 features corresponding to sonar returns bounced off different surfaces. Each sample is labeled as either 'M' (Mine) or 'R' (Rock).
